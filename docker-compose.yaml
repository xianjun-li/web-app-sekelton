version: '3.9'

services:

  php:
    restart: always
    build:
      context: ./
      dockerfile: ./dockers/php-fpm-7
    volumes:
      - ./src:/var/www/

  caddy:
    restart: always
    build:
      context: ./
      dockerfile: ./dockers/caddy
    volumes:
      - ./src:/var/www/
